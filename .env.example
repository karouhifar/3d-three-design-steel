# Where /api/quote forwards validated lead + building config.
# Leave unset in dev: the route validates, logs, and returns ok (no forward).
QUOTE_BACKEND_URL=
# Optional bearer token sent as `Authorization: Bearer <token>` to the backend.
QUOTE_BACKEND_TOKEN=
